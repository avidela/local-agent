[project]
name = "local-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.1.0",
]

[tool.uv.workspace]
members = [
    "apps/frontends/streamlit",
    "apps/backends/adk",
]
